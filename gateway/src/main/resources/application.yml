server:
  port: 8090
spring:
  reactor:
    context-propagation: auto
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
app:
  hostname: ${HOST}
