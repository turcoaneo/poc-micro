server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/turcoaneo/poc-server-config
          default-label: main
  application:
    name: config-server

eureka:
  client:
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/